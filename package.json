{
  "name": "ai-agent-framework",
  "version": "1.0.0",
  "private": true,
  "description": "AI Agent Framework - Production-ready AI agent with LLM support, RAG, and tool calling",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "clean": "pnpm -r --filter='./packages/*' run clean",
    "test": "pnpm -r --filter='./packages/*' run test",
    "typecheck": "pnpm -r --filter='./packages/*' run typecheck"
  },
  "keywords": [
    "ai",
    "agent",
    "llm",
    "framework",
    "rag",
    "tool-calling"
  ],
  "author": "AI Agent Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
