FROM node:20-alpine

# 安装 pnpm
RUN corepack enable && corepack prepare pnpm@9 --activate

# 设置工作目录
WORKDIR /workspace

# 复制项目文件
COPY . .

# 设置环境变量
ENV CI=true
ENV NODE_ENV=test

# 默认命令
CMD ["/bin/sh"]
