# 贡献指南

感谢你对 Agentic 的关注！本文档提供了贡献的指南和说明。

## 行为准则

参与本项目即表示你同意遵守我们的[行为准则](CODE_OF_CONDUCT.md)。

## 如何贡献？

### 报告 Bug

在创建 bug 报告之前，请检查现有的 issues 以避免重复。创建 bug 报告时，请包含尽可能多的细节：

- 使用清晰且描述性的标题
- 描述重现问题的确切步骤
- 提供具体的示例和代码样本
- 描述你观察到的行为以及你期望的行为
- 包含你的环境详情（Node.js 版本、操作系统等）

### 建议新功能

欢迎功能建议！请：

- 使用清晰且描述性的标题
- 提供功能的详细描述
- 解释为什么这个功能会有用
- 包含功能使用方式的代码示例

### Pull Requests

1. **Fork 仓库**并从 `main` 创建你的分支
2. **安装依赖**: `pnpm install`
3. **进行更改**并遵循我们的编码标准
4. **为新功能添加测试**
5. **确保测试通过**: `pnpm test`
6. **运行代码检查**: `pnpm run lint`
7. **格式化代码**: `pnpm run format`
8. **更新文档**（如需要）
9. **使用约定式提交**提交你的更改
10. **推送到你的 fork**并提交 pull request

## 开发设置

### 前置要求

- Node.js 18.x 或更高版本
- pnpm 9.x 或更高版本

### 安装

```bash
# 克隆你的 fork
git clone https://github.com/your-username/agentic.git
cd agentic

# 安装依赖
pnpm install

# 构建所有包
pnpm run build

# 运行测试
pnpm run test
```

### 项目结构

```
packages/
├── core/              # 核心智能体框架
├── storage-memory/    # 内存存储适配器
├── storage-prisma/    # Prisma 存储适配器
└── cli/               # CLI 脚手架工具

.github/               # GitHub workflows 和模板
docs/                  # 文档
```

## 编码标准

### TypeScript

- 使用 TypeScript 编写所有代码
- 启用严格模式
- 为所有公共 API 提供类型定义
- 使用有意义的变量和函数名

### 代码风格

我们使用 ESLint 和 Prettier 进行代码格式化：

```bash
# 检查代码规范
pnpm run lint

# 修复代码规范问题
pnpm run lint:fix

# 格式化代码
pnpm run format
```

### 提交信息

我们遵循[约定式提交](https://www.conventionalcommits.org/zh-hans/)：

```
feat: 添加新功能
fix: 修复组件中的 bug
docs: 更新文档
test: 为功能添加测试
refactor: 重构代码
chore: 更新依赖
```

示例：
- `feat(plugin): 为插件添加生命周期钩子`
- `fix(llm): 正确处理超时错误`
- `docs(api): 更新 Agent 类文档`

## 测试

### 运行测试

```bash
# 运行所有测试
pnpm test

# 运行测试并监听变化
pnpm test:watch

# 运行测试并生成覆盖率报告
pnpm test:coverage
```

### 编写测试

- 为所有新功能编写单元测试
- 为关键组件编写基于属性的测试
- 使用描述性的测试名称
- 遵循现有的测试模式

示例：

```typescript
import { describe, it, expect } from 'vitest';
import { Agent } from '../agent';

describe('Agent', () => {
  it('应该正确处理消息', async () => {
    const agent = new Agent({...});
    const response = await agent.chat('你好');
    expect(response.content).toBeDefined();
  });
});
```

### 基于属性的测试

我们使用 `fast-check` 进行基于属性的测试：

```typescript
import { fc, test } from '@fast-check/vitest';

test.prop([fc.string(), fc.array(fc.object({...}))])(
  '属性描述',
  async (message, history) => {
    // 测试实现
  }
);
```

## 文档

### 代码文档

- 为所有公共 API 添加 JSDoc 注释
- 包含参数描述和返回类型
- 在注释中提供使用示例

示例：

```typescript
/**
 * 处理用户消息并生成响应
 *
 * @param message - 用户的消息
 * @param options - 可选配置
 * @returns 包含内容和元数据的智能体响应
 *
 * @example
 * ```typescript
 * const response = await agent.chat('你好', {
 *   history: previousMessages
 * });
 * ```
 */
async chat(message: string, options?: ChatOptions): Promise<AgentResponse>
```

### 文档文件

- 为面向用户的更改更新 README.md
- 为 API 更改更新 API.md
- 为破坏性更改更新 MIGRATION.md
- 为新功能添加示例

## 发布流程

发布使用 semantic-release 自动化：

1. 将 PR 合并到 `main` 分支
2. CI 运行测试和构建
3. semantic-release 分析提交
4. 自动提升版本
5. 生成 Changelog
6. 发布包到 npm
7. 创建 GitHub release

## 分支保护规则

`main` 分支受以下规则保护：

- 合并前需要 pull request 审查
- 合并前需要状态检查通过
- 合并前需要分支保持最新
- 需要线性历史
- 管理员也受限制

## 获取帮助

- 查看[文档](https://chajian.github.io/agentic/)
- 搜索[现有 issues](https://github.com/Chajian/agentic/issues)
- 在[讨论区](https://github.com/Chajian/agentic/discussions)提问

## 致谢

贡献者将被认可在：
- CHANGELOG.md 中的贡献
- README.md 贡献者部分
- GitHub 贡献者页面

感谢你的贡献！🎉
